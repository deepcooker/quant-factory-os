# tools/backup

This directory stores archived compatibility assets that are no longer part of the formal mainline.

Current archived shell entrypoints:
- `legacy.sh`
- `task.sh`
- `observe.sh`
- `ship.sh`

Archived Python assets:
- `run_a9.py`

The original paths under `tools/` are thin wrappers during the transition period.
