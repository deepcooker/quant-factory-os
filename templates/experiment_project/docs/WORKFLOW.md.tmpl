# WORKFLOW

## Purpose
This file describes the minimal experimental-line workflow for `__PROJECT_NAME__`.

## Bootstrap workflow
1. Fill owner truth in `docs/PROJECT_GUIDE.md`
2. Register repo-local skills under `.agents/skills/`
3. Use `tools/project_config.json` as the local experiment-line config
4. Generate evidence into `state/`, `reports/`, `artifacts/`, `logs/`

## Notes
- This is a bootstrap skeleton, not a finished project workflow.
