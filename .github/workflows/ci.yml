name: ci
on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Repo skeleton checks
        run: |
          set -e
          test -f CHARTER.md
          test -d TASKS
          test -d ISSUES
          echo "✅ skeleton ok"
