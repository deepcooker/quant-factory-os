refresh-baseline: 接下来请你把 current summary 回灌到 baseline 学习模型。

目标：
- 不重复构建整个 baseline
- 只吸收本轮工作中真正有效的增量认知
- 更新 baseline 的主线理解、摘要和下一步

要求：
- 只使用 current summary 中的有效结论
- 不把工作 session 噪音直接带回 baseline
- 输出必须可直接写回 baseline 学习快照
