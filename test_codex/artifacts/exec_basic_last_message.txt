1. Establish execution context:
- Select an active `TASK_ID` from `TASKS/TASK-*.md` (or next unchecked in `TASKS/QUEUE.md`), and ensure `TASK_ID` + `RUN_ID` are set in `TASKS/STATE.md`.

2. Run mandatory session gate (before any implementation):
- `tools/qf init`
- `tools/qf learn -log` (must show `MODEL_SYNC=1`, `PLAN_MODE=strong`, and required `LEARN_*` anchors)
- `tools/qf ready`

3. Plan and confirm scope:
- Read task acceptance criteria, map to smallest safe diff, and follow `Plan -> Confirm -> Execute`.
- If direction is unclear/complex, use the discussion lane: `orient -> choose -> council -> arbiter -> slice`.

4. Execute and verify:
- `make evidence RUN_ID=<RUN_ID>`
- Implement minimal change set
- `make verify` until green (or `pytest -q` when appropriate)

5. Record evidence and ship readiness:
- Update `reports/<RUN_ID>/meta.json`, `summary.md`, `decision.md` (including stop reason, commands, artifacts).
- If workflow/rules changed, update required owner docs in the same run.
- Prepare PR with `RUN_ID` in title and `Why/What/Verify/Evidence` in body.

No files were modified.